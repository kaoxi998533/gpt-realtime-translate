plugins {
    id "com.android.application"
}

android {
    namespace "com.example.realtimetranslate"
    compileSdk 36

    defaultConfig {
        applicationId "com.example.realtimetranslate"
        minSdk 26
        targetSdk 36
        versionCode 1
        versionName "0.1.0"
    }

    sourceSets {
        main {
            assets.srcDirs = ["../../public"]
        }
    }
}
